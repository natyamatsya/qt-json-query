# cmake
/build*
CMakeUserPresets.json

# machine-specific Qt paths for Init-DevEnv.ps1 (see qt.user.example.json)
qt.user.json

# jetbrains
.idea

# Local working notes / implementor feedback drops (not part of the repo)
context/
